**Goal:** Combine data from different sources into one coherent dataset.

### Sub-steps

*   Merge datasets (joins, unions)
    
*   Resolve schema conflicts (column names, formats)
    
*   Resolve entity conflicts (same user, different IDs)
    
*   Remove redundant attributes
    

**Outcome:** Single, consistent data view.